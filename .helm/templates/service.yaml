apiVersion: v1
kind: Service
metadata:
  name: {{ include "flts.fullname" . }}
  labels:
    {{- include "flts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flts.selectorLabels" . | nindent 4 }}
