apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flts.fullname" . }}-test-connection"
  labels:
    {{- include "flts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
